# Environment

## 源文件地址
`e:\develop\heroes\vcmi-assets\vcmi-client\include\vcmi\Environment.h`

## 概述
`Environment`定义了VCMI引擎中的环境接口。该接口提供对各种服务的访问，允许组件获取它们运行所需的服务实例。

## 类定义

### Environment
环境接口，提供对各种服务的访问方法。